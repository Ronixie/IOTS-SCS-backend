server:
  port: 8080
spring:
  application:
    name: gateway
  cloud:
    nacos:
      server-addr: ${NACOS_IP:127.0.0.1}:${NACOS_PORT:8848} # nacos地址,NACOS_IP环境变量,NACOS_PORT环境变量
  main:
    web-application-type: reactive
